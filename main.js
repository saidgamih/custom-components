// Notification actions

show_notification.addEventListener("click", show);
hide_notification.addEventListener("click", hide);

// Message box actions

show_message_box.addEventListener("click", showMsgBox);
hide_message_box.addEventListener("click", hideMsgBox);